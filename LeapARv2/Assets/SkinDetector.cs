using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using RedMatter.SkinDetection;

public class SkinDetector : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //BaseDetector detector = new BaseDetector(sourceImage, new SimpleSkinDetector1());
        //pbxImage.Image = detector.SkinDetectionImage;
    }
}
